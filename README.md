# Employee Service Frontend

React + Vite frontend for the employee service request portal.

## Run locally

```powershell
npm install
npm run dev
```

By default, the app calls the backend at `http://localhost:8080`.
Set `VITE_API_BASE_URL` to point to a different backend URL.

